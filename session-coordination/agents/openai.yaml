interface:
  display_name: "Session Coordination"
  short_description: "Inspect and message exact local Codex sessions"
  default_prompt: "Use $session-coordination to inspect bounded native session state or queue a message to one exact shared local Codex session while preserving runtime and delivery semantics."
policy:
  allow_implicit_invocation: true
