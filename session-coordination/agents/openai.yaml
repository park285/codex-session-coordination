interface:
  display_name: "Session Coordination"
  short_description: "Inspect and message exact local Codex sessions"
  default_prompt: "Use $session-coordination to inspect bounded native session state or send a message to one exact shared local Codex session, preferring steering and queuing only when steering is confirmed unavailable while preserving uncertain delivery outcomes."
policy:
  allow_implicit_invocation: true
